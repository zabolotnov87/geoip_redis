require 'spec_helper'

describe GeoipRedis do
  it 'has a version number' do
    expect(GeoipRedis::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
